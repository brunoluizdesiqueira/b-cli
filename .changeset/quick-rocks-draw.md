---
"@brunoluizdesiqueira/bbuilder-cli": major
---

Resolver templates ${envVersion} e ${libRoot} dentro do dependencyPaths do config. Antes, paths com placeholders eram usados literalmente sem substituição.

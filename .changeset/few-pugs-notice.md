---
"@brunoluizdesiqueira/bbuilder-cli": patch
---

Add environment diagnostics with `bbuilder doctor`, configuration validation with `bbuilder config validate`, and stronger terminal help examples. Also prepare automated CI/release workflow using Changesets.
